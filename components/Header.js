import React from "react"
import ReactDOM from "react-dom"

export default function Header() {
    return (
        <header>
            <img id = 'header-img' src = './images/World_icon.png' />
            <h2 id = 'header-title'>my travel journal.</h2>
        </header>
    )
}